import { Card } from './Card';

export const ForecastPerHour = data => {
  return (
    <div className="container">
      <ul className="forecastPerHour--list">
        <Card />
      </ul>
    </div>
  );
};
